services:
  web:
    image: python:3.12-alpine
    working_dir: /lab
    command:
      - python
      - -m
      - http.server
      - "8000"
    ports:
      - "8000:8000"
    volumes:
      - .:/lab:ro
    restart: unless-stopped
